/*
Arquivo: backend/src/dataset.c
Funcao no projeto: implementar carga e ordenacao do dataset.
Conteudo: stubs de carregar_dataset_exemplo e ordenar_locais_por_id.
O que faz: hoje apenas marca os pontos onde a equipe vai implementar dados reais.
*/
#include "dataset.h"

size_t carregar_dataset_exemplo(Local *destino, size_t capacidade) {
    (void)destino;
    (void)capacidade;

    /* Ambiente pronto: dataset real sera definido pela equipe. */
    return 0;
}

void ordenar_locais_por_id(Local *locais, size_t total) {
    (void)locais;
    (void)total;

    /* Ambiente pronto: ordenacao sera implementada pela equipe. */
}
